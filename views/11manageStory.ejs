<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Stories</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: #3b3f47;
      color: white;
      padding: 10px 0;
      text-align: center;
    }
    h1 {
      margin: 0;
      font-size: 36px;
    }
    .container {
      width: 90%;
      max-width: 1000px;
      margin: 20px auto;
      padding: 20px;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .add-story-btn {
      display: inline-block;
      background-color: #5cb85c;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 4px;
      margin-bottom: 20px;
      font-weight: bold;
    }
    .add-story-btn:hover {
      background-color: #4cae4c;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      background-color: #fafafa;
      border: 1px solid #ddd;
      margin: 10px 0;
      padding: 20px;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
    }
    li h2 {
      margin: 0;
      font-size: 24px;
      color: #333;
      flex: 1; /* Phần chữ chiếm không gian còn lại */
    }
    li p {
      margin: 5px 0;
      color: #666;
    }
    .image-container {
      flex-shrink: 0; /* Không co lại */
      display: flex;
      flex-direction: column; /* Sắp xếp hình ảnh và icon theo cột */
      gap: 10px; /* Khoảng cách giữa hình ảnh và icon */
      align-items: center; /* Căn giữa các phần tử */
    }
    .image-container img {
      max-width: 150px;
      max-height: 150px;
      border-radius: 8px;
      object-fit: cover;
    }
    .actions {
      display: flex;
      gap: 15px;
      justify-content: flex-start;
    }
    .actions a {
      background-color: #f0ad4e;
      color: white;
      padding: 8px 16px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
    }
    .actions a.delete {
      background-color: #d9534f;
    }
    .actions a:hover {
      opacity: 0.8;
    }
    .actions a.delete:hover {
      background-color: #c9302c;
    }
  </style>
</head>
<body>
    <%- include('partials/navbar') %>

  <div class="container">
    <a href="/stories/add" class="add-story-btn">Add New Story</a>

    <ul>
      <% stories.forEach(story => { %>
        <li>
          <!-- Phần chữ (Tên câu chuyện và trạng thái) -->
          <div class="text-container">
            <h2><%= story.name %></h2>
            <p><strong>Status:</strong> <%= story.isViewed ? 'Viewed' : 'Not Viewed' %></p>
          </div>

          <!-- Phần hình ảnh và icon -->
          <div class="image-container">
            <div>
              <strong>Image:</strong>
              <img src="<%= story.imageFileName %>" alt="<%= story.name %> Image">
            </div>
            <div>
              <strong>Icon:</strong>
              <img src="<%= story.iconFileName %>" alt="<%= story.name %> Icon">
            </div>
          </div>

          <!-- Các hành động: Edit và Delete -->
          <div class="actions">
            <a href="/stories/edit/<%= story._id %>">Edit</a>
            <a href="/stories/delete/<%= story._id %>" class="delete">Delete</a>
          </div>
        </li>
      <% }) %>
    </ul>
  </div>

</body>
</html>
