<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Stories</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
    }
    header {
      background-color: #3b3f47;
      color: white;
      padding: 10px 0;
      text-align: center;
    }
    h1 {
      margin: 0;
      font-size: 36px;
    }
    .container {
      width: 90%;
      max-width: 1000px;
      margin: 20px auto;
      padding: 20px;
      background-color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .add-story-btn {
      display: inline-block;
      background-color: #5cb85c;
      color: white;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 4px;
      margin-bottom: 20px;
      font-weight: bold;
    }
    .add-story-btn:hover {
      background-color: #4cae4c;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    li {
      background-color: #fafafa;
      border: 1px solid #ddd;
      margin: 10px 0;
      padding: 15px;
      border-radius: 8px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    li h2 {
      margin: 0 0 10px;
      font-size: 24px;
      color: #333;
    }
    li p {
      margin: 5px 0;
      color: #666;
    }
    .actions {
      display: flex;
      gap: 10px;
    }
    .actions a {
      background-color: #f0ad4e;
      color: white;
      padding: 8px 16px;
      text-decoration: none;
      border-radius: 4px;
      font-weight: bold;
    }
    .actions a.delete {
      background-color: #d9534f;
    }
    .actions a:hover {
      opacity: 0.8;
    }
    .actions a.delete:hover {
      background-color: #c9302c;
    }
  </style>
</head>
<body>
    <%- include('partials/navbar') %>



  <div class="container">
    <a href="/stories/add" class="add-story-btn">Add New Story</a>

    <ul>
      <% stories.forEach(story => { %>
        <li>
          <h2><%= story.name %></h2>
          <p><strong>Image:</strong> <%= story.imageFileName %></p>
          <p><strong>Icon:</strong> <%= story.iconFileName %></p>
          <p><strong>Status:</strong> <%= story.isViewed ? 'Viewed' : 'Not Viewed' %></p>
          <div class="actions">
            <a href="/stories/edit/<%= story._id %>">Edit</a>
            <a href="/stories/delete/<%= story._id %>" class="delete">Delete</a>
          </div>
        </li>
      <% }) %>
    </ul>
  </div>

</body>
</html>
